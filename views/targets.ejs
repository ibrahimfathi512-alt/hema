<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Target Analysis | Tanta Car Elite</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700;900&display=swap" rel="stylesheet">
    <style>
        :root {
            --tanta-orange: #ff5e00;
            --main-dark: #09090b;
            --card-bg: rgba(23, 23, 26, 0.8);
            --border-color: rgba(255, 255, 255, 0.08);
            --text-muted: #a1a1aa;
            --success: #22c55e;
            --warning: #f1c40f;
            --danger: #ef4444;
        }

        body {
            background-color: var(--main-dark);
            background-image: radial-gradient(circle at 20% 20%, rgba(255, 94, 0, 0.05) 0%, transparent 40%);
            color: #fff;
            font-family: 'Cairo', sans-serif;
            margin: 0;
            padding: 30px;
            min-height: 100vh;
        }

        /* Nav Bar */
        .nav-header {
            background: var(--card-bg);
            backdrop-filter: blur(20px);
            border: 1px solid var(--border-color);
            padding: 20px 30px;
            border-radius: 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }

        .nav-header h2 { margin: 0; font-size: 22px; font-weight: 800; }
        .back-btn {
            text-decoration: none;
            color: var(--tanta-orange);
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 10px;
            transition: 0.3s;
        }
        .back-btn:hover { transform: translateX(5px); filter: brightness(1.2); }

        /* Stats Grid */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 40px;
        }

        .stat-card {
            background: var(--card-bg);
            border: 1px solid var(--border-color);
            padding: 25px;
            border-radius: 24px;
            position: relative;
            overflow: hidden;
            transition: 0.4s;
        }
        .stat-card:hover { border-color: var(--tanta-orange); transform: translateY(-5px); }
        .stat-card h4 { color: var(--text-muted); margin: 0; font-size: 14px; text-transform: uppercase; }
        .stat-card h2 { margin: 15px 0 0 0; font-size: 32px; font-weight: 900; }
        .stat-card i {
            position: absolute;
            left: 20px;
            top: 20px;
            font-size: 40px;
            opacity: 0.1;
            color: var(--tanta-orange);
        }

        /* Table Design */
        .table-container {
            background: var(--card-bg);
            border: 1px solid var(--border-color);
            border-radius: 24px;
            overflow: hidden;
            box-shadow: 0 20px 40px rgba(0,0,0,0.4);
        }

        table { width: 100%; border-collapse: collapse; }
        th {
            background: rgba(255,255,255,0.03);
            color: var(--text-muted);
            padding: 20px;
            text-align: center;
            font-size: 13px;
            text-transform: uppercase;
            border-bottom: 1px solid var(--border-color);
        }
        td { padding: 18px; text-align: center; border-bottom: 1px solid rgba(255,255,255,0.02); font-weight: 600; }

        /* Progress Bar */
        .progress-wrapper { width: 150px; margin: 0 auto; }
        .progress-bar {
            height: 6px;
            background: rgba(255,255,255,0.05);
            border-radius: 10px;
            overflow: hidden;
            margin-top: 8px;
        }
        .progress-fill { height: 100%; border-radius: 10px; transition: 1s ease-out; }

        /* Badges */
        .badge {
            padding: 6px 15px;
            border-radius: 12px;
            font-size: 12px;
            font-weight: 800;
            display: inline-flex;
            align-items: center;
            gap: 6px;
        }
        .bg-excellent { background: rgba(34, 197, 94, 0.15); color: var(--success); border: 1px solid rgba(34, 197, 94, 0.2); }
        .bg-good { background: rgba(241, 196, 15, 0.15); color: var(--warning); border: 1px solid rgba(241, 196, 15, 0.2); }
        .bg-poor { background: rgba(239, 68, 68, 0.15); color: var(--danger); border: 1px solid rgba(239, 68, 68, 0.2); }

        /* Glow effects */
        .glow-text-success { text-shadow: 0 0 15px rgba(34, 197, 94, 0.4); color: var(--success); }
    </style>
</head>
<body>

    <div class="nav-header">
        <div style="display: flex; align-items: center; gap: 15px;">
            <div style="width: 10px; height: 10px; border-radius: 50%; background: var(--tanta-orange); box-shadow: 0 0 10px var(--tanta-orange);"></div>
            <h2>تحليل تارجت الزون المباشر: <%= zone %></h2>
        </div>
        <a href="/dashboard" class="back-btn">
            العودة للرئيسية
            <i class="fa-solid fa-house"></i>
        </a>
    </div>

    <div class="stats-grid">
        <div class="stat-card">
            <i class="fa-solid fa-bullseye"></i>
            <h4>التارجت المطلوب</h4>
            <h2><%= cleanData(zoneData.get('normal Target')) %> <span style="font-size: 14px; color: var(--text-muted);">ساعة</span></h2>
        </div>
        <div class="stat-card">
            <i class="fa-solid fa-chart-simple"></i>
            <h4>المتوسط الحالي</h4>
            <h2><%= cleanData(zoneData.get('Average')) %> <span style="font-size: 14px; color: var(--text-muted);">ساعة</span></h2>
        </div>
        <div class="stat-card" style="border-right: 4px solid var(--success);">
            <i class="fa-solid fa-gauge-high" style="color: var(--success);"></i>
            <h4>نسبة التحقيق الإجمالية</h4>
            <h2 class="glow-text-success"><%= zoneData.get('Average %') %></h2>
        </div>
    </div>

    <div class="table-container">
        <table>
            <thead>
                <tr>
                    <th>التاريخ</th>
                    <th>الساعات المحققة</th>
                    <th>نسبة الإنجاز</th>
                    <th>حالة الأداء اليومي</th>
                </tr>
            </thead>
            <tbody>
                <% headers.forEach(h => { 
                    if(h.includes('/')) { 
                        let val = cleanData(zoneData.get(h));
                        let target = cleanData(zoneData.get('normal Target'));
                        let pct = (target > 0) ? (val / target * 100) : 0;
                        
                        let badgeClass = '';
                        let barColor = '';
                        let statusText = '';
                        let icon = '';

                        if (pct >= 90) { 
                            badgeClass = 'bg-excellent'; barColor = 'var(--success)'; 
                            statusText = 'عاش يا بطل استمر'; icon = 'fa-trophy'; 
                        }
                        else if (pct >= 80) { 
                            badgeClass = 'bg-good'; barColor = 'var(--warning)'; 
                            statusText = 'أداء مستقر'; icon = 'fa-star'; 
                        }
                        else { 
                            badgeClass = 'bg-poor'; barColor = 'var(--danger)'; 
                            statusText = 'اداء سئ'; icon = 'fa-circle-exclamation'; 
                        }
                %>
                <tr>
                    <td style="color: var(--text-muted); font-family: monospace;"><%= h %></td>
                    <td style="font-size: 18px;"><%= val %> <small style="font-size: 10px; opacity: 0.5;">HR</small></td>
                    <td>
                        <div class="progress-wrapper">
                            <div style="display:flex; justify-content:space-between; font-size: 11px; margin-bottom: 4px;">
                                <span><%= pct.toFixed(1) %>%</span>
                            </div>
                            <div class="progress-bar">
                                <div class="progress-fill" style="width: <%= pct %>%; background: <%= barColor %>; box-shadow: 0 0 10px <%= barColor %>66;"></div>
                            </div>
                        </div>
                    </td>
                    <td>
                        <span class="badge <%= badgeClass %>">
                            <i class="fa-solid <%= icon %>"></i>
                            <%= statusText %>
                        </span>
                    </td>
                </tr>
                <% } }); %>
            </tbody>
        </table>
    </div>

    <div style="margin-top: 30px; text-align: center; color: var(--text-muted); font-size: 12px; letter-spacing: 1px;">
        TANTA CAR FLEET ANALYTICS // v2.6 // SECURE SESSION
    </div>

</body>
</html>